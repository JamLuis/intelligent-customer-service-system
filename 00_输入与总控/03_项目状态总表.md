# Intelligent Customer Service System 项目状态总表

## 1. 阶段状态

| 阶段 | 状态 | 开始时间 | 最近更新时间 | 负责人 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 需求梳理 | 已完成 | 2026-05-22 | 2026-05-22 | Copilot 初始化 | 已完成缺口分析与固定格式需求输入 |
| 业务流程 | 已完成 | 2026-05-22 | 2026-05-22 | Copilot 初始化 | 已输出主流程、异常流程、边界流程 |
| 产品设计 | 已更新 | 2026-05-22 | 2026-05-23 | Copilot 初始化 | 已根据问答页面草稿补齐执行日志、图谱治理、知识录入、MCP 管理、路径固化/重建，并新增 Gate 冻结评审清单 |
| 技术设计 | 已更新 | 2026-05-22 | 2026-05-23 | Copilot 初始化 | 已升级为 Unified Engineering Knowledge Graph / Multi Source Engineering Graph 架构，新增 07E 项目框架；已补充 07F 真实知识图谱构建与检索详细设计；已明确 `uncategorized / 待归类资料` 兜底分类和搜索来源证据字段；已补充 07H 知识库问答、模型配置隔离和强制关系图谱回答开关；存在 PM-FB-001~007、BIZ-Q-001~004、KG-FB-001~005 待确认 |
| 数据库设计 | 已完成 | 2026-05-23 | 2026-05-23 | Copilot 初始化 | 已完成 V0.1 P0 数据库设计，包含 15 张核心表和每表独立建表 SQL；真实接口、审批流、长期留存策略保留为条件约束 |
| API 设计 | 已完成 | 2026-05-23 | 2026-05-23 | Copilot 初始化 | 已完成 V0.1 P0 API 契约、统一接口规范、错误码登记、Mock 场景、字段映射、MCP 能力与 SSE 事件契约；真实 wnx 接入后续按 capability 替换 |
| Agent 任务规划 | 已更新 | 2026-05-22 | 2026-05-23 | Copilot 初始化 | 已输出主任务书与阶段任务顺序；已新增真实知识图谱构建与检索专项任务包，可直接分派 DB/API/Java/Python/前端/测试 Agent |
| 开发实施 | 进行中 | 2026-05-22 | 2026-05-23 | Copilot 初始化 | 已完成初始化工程骨架、基础设施 Docker 验证脚本、Java Mock API、用户诊断与后台管理前端、知识录入预览、历史图谱动态分类维护、Python AI Service Mock 边界、Node MCP 工具调用边界，以及真实 KG source -> parse -> extract -> graph build -> Neo4j draft 最小闭环；已补齐待归类兜底、历史图谱源文件/证据块回看、搜索结果来源字段、模型 Profile 隔离和知识库问答最小闭环 |
| 测试验证 | 进行中 | 2026-05-22 | 2026-05-23 | Copilot 初始化 | 已验证 PostgreSQL + pgvector、Neo4j、Redis、真实 KG 新增表与 taxonomy、Java 后端测试、Python 语法检查、前端构建、MCP 构建、Java -> Python AI -> Node MCP 跨服务 smoke、一键启动前端 5173 端口、真实 KG 数据端到端 smoke，以及待归类 source、历史图谱 source/evidence 面板、诊断搜索来源字段、知识库问答新增代码编译构建 |
| 发布上线 | 未开始 |  | 2026-05-22 | 待指定 | 非本轮范围 |

## 2. 当前进行中任务

| 任务编号 | 任务名称 | 状态 | 输入文档 | 输出结果 | 负责人 |
| --- | --- | --- | --- | --- | --- |
| T-001 | 新项目初始化与 PM 初版固化 | 已完成 | 用户规划说明 | PM 文档、状态文档、任务文档 | Copilot 初始化 |
| T-002 | 分层工程骨架创建 | 已完成 | PM 初版与项目总控 | frontend、backend-java、ai-service-python、mcp-server-node、infra | Copilot 初始化 |
| T-003 | 技术方案与架构落盘 | 已完成 | PM 文档、用户架构输入 | 07、07A、07B、07C、07D | Copilot 初始化 |
| T-005 | 统一工程知识图谱框架重构 | 已完成 | 用户最新 Multi Source Engineering Graph 输入 | 07E、07/07A/07B/07C/07D 增量更新、状态风险决策同步 | Copilot 初始化 |
| T-004 | 真实接口清单与权限复用澄清 | 阻塞 | 架构文档、风险清单 | API 文档输入、MCP 契约输入 | 待指定 |
| T-006 | 问答图谱 MCP 闭环产品补全 | 已完成 | 用户问答页面草稿、PM 初版文档 | 06 产品规划补充、04/04B/04C/04D/05 增量更新 | Copilot 初始化 |
| T-007 | 数据库/API 设计前 Gate 冻结评审 | 已形成临时结论 | PM 产物、架构文档、风险阻塞清单 | 07_Gate冻结评审清单 GQ-001~GQ-024 临时冻结结论 | PM / 架构负责人 / 目标系统负责人 |
| T-008 | 数据库/API 设计启动输入整理 | 已完成 | Gate 临时结论、PM 产物、架构文档 | 04_数据库与API/00_数据库API设计启动输入.md | Copilot 初始化 |
| T-009 | V0.1 P0 数据库设计 | 已完成 | 04_数据库与API/00_数据库API设计启动输入.md、04C 状态枚举 | 04_数据库与API/08_数据库设计.md、04_数据库与API/08A_建表SQL/*.sql | Copilot 初始化 |
| T-010 | V0.1 P0 API 契约设计 | 已完成 | 04_数据库与API/00_数据库API设计启动输入.md、04D 接口需求、08 数据库设计 | 04_数据库与API/09_API文档.md、09B_接口规范.md、09C_错误码登记表.md、09D~09I 辅助契约 | Copilot 初始化 |
| T-011 | Docker 基础设施部署验证 | 已完成 | infra/docker-compose.yml、infra/postgres/init.sql、08A 建表 SQL | scripts/verify-infra.sh；PostgreSQL 扩展与 15 张表、Neo4j、Redis 验证通过并已关闭容器 | Copilot 初始化 |
| T-012 | Java 主后台 Mock API 实现 | 已完成 | 09_API文档.md、09B_接口规范.md、09C_错误码登记表.md | backend-java Controller、统一响应、错误处理、内存 Mock Service、CORS；关键链路 smoke 通过 | Copilot 初始化 |
| T-013 | 前端联调工作台实现 | 已完成 | 09_API文档.md、后端 Mock API | frontend Vue3 + Vite + TypeScript + Element Plus 工程、API client、问答诊断/执行轨迹/知识录入/MCP/路径反馈页面；构建通过 | Copilot 初始化 |
| T-014 | Python AI Service Mock 边界实现 | 已完成 | 07E、07B、09_API文档.md | ai-service-python 提供 `/diagnosis/run`、`/knowledge/ingest`、`/graphs/query`，Java 后端可调用并回退本地 Mock | Copilot 初始化 |
| T-015 | Node MCP 工具调用边界实现 | 已完成 | 07B、09_API文档.md | mcp-server-node 提供 `/manifest`、`/tools`、`/tools/{name}/invoke`，Java 后端可同步能力并调用 `device.getStatus` | Copilot 初始化 |
| T-016 | 前端组件化与启动脚本修复 | 已完成 | 用户反馈、frontend 当前实现、scripts/start-all.sh | frontend 拆分为 router/views/components；一键启动自动选择 Node 20+/22+，已验证 5173 端口监听 | Copilot 初始化 |
| T-017 | 前端产品形态重构 | 已完成 | 用户最新前端产品建议 | `/chat` 用户诊断界面；`/admin/knowledge` 知识图谱维护；`/admin/tickets` 问题工单；`/admin/mcp-tools` MCP 工具库 | Copilot 初始化 |
| T-018 | 知识图谱维护交互升级 | 已完成 | 用户要求支持结构化文本、非结构化文件和关系图编辑 | 引入 Vue Flow；知识录入拆分为结构化文本/非结构化文件；图谱维护支持实体拖拽、删除实体/关系、重建关系和保存草稿 | Copilot 初始化 |
| T-019 | 一键启动 ready 等待修复 | 已完成 | 用户反馈一键启动后前端 5173 仍不可访问 | `start-all.sh` 启动后等待 MCP、AI、Java 后端和前端 HTTP ready；`status` 显示 PID 与 HTTP ready 状态 | Copilot 初始化 |
| T-020 | 知识图谱分类治理升级 | 已完成 | 用户要求知识图谱拆为录入预览与历史维护，并支持大量动态图谱分类 | 后端新增图谱分类元数据与分类筛选；DB 设计补充 graph_category/entity_types/relation_types；前端拆分 `/admin/knowledge/ingest` 与 `/admin/knowledge/graphs` | Copilot 初始化 |
| T-021 | 真实知识图谱构建与检索设计 | 已完成 | 用户要求结构化文本和非结构化文本必须形成真实可检索关系图谱 | `03_技术方案与架构/07F_真实知识图谱构建与检索详细设计.md`，覆盖 Neo4j、PostgreSQL、pgvector、抽取、复核、版本、检索 API 与 Agent 编码任务 | Copilot 初始化 |
| T-022 | 真实知识图谱专项 Agent 任务包 | 已完成 | `03_技术方案与架构/07F_真实知识图谱构建与检索详细设计.md` | `05_开发计划与Agent任务/21_真实知识图谱构建与检索任务包/`，包含里程碑、任务总表、DB/API/Java/Python/前端/测试任务书、模块技术设计和派单提示词 | Copilot 技术负责人 |
| T-023 | 真实 KG 数据库底座开发 | 已完成 | KG-DB-001~003、数据库 Agent 任务书 | 新增 `real_kg_schema.sql`、`graph_taxonomy_seed.sql`，接入 `init.sql` 与 `verify-infra.sh`，完成真实 KG 表、taxonomy seed 和 pgvector 索引验证 | Copilot 数据库执行 |
| T-024 | 真实 KG API 契约补充 | 已完成 | KG-API-001~002、接口 Agent 任务书 | 新增 `09J_真实知识图谱API补充契约.md`，补齐 KG-001~KG-015、KG 错误码和接口交接单 | Copilot 接口执行 |
| T-025 | 真实 KG 后端基础查询与知识源元数据落地 | 已完成 | KG-BE-001、KG-BE-002、KG-BE-003（部分）、后端 Java Agent 任务书 | 接入 Spring JDBC 和 PostgreSQL 驱动，落地 taxonomy、graph asset、knowledge source/task/blocks/candidates 的真实查询与基础写入，新增后端交接单 | Copilot 后端执行 |
| T-026 | 知识图谱本体通用化重构 | 已完成 | 用户明确要求"不能在建表和系统设置实体将这些写死" | 重写 07F（§1A/§7等）、重写 09J（加 KG-016〜021）、拆除 graph_taxonomy_seed.sql，新增 graph_protected_taxonomy.sql + examples/，同步 init.sql、verify-infra.sh、任务包 03/06/08/12/02 | Copilot 重构执行 |
| T-027 | 脏代码扫除与 07F 解析实现细节补全 | 已完成 | 用户明确"系统绝对不能有偏离正确的设计走向 导致后期脏代码和文件造成上下文污染"，并要求详述非结构化文件→blocks→candidates 实现 | 07F 新增 §10A 端到端实现章节（含时序图、Parser/Embedding/Extract/Normalize/写库失败矩阵）；MockSupportService 重写为零业务本体（478→288 行，移除 graph 类目/资产/知识源/seedRoute 等死代码与 TC-003/MINX/AR-17/BOUND_TO/RAISED_BY/INSTALLED_ON 等所有海事字面量）；ai-service-python/app/main.py 重写为接口 stub（parse/embed/extract/normalize/retrieve-plan 五个接口，明确指向 KG-AI-001~005），删除 5 个空占位子模块；frontend GraphEditor.vue / ChatView.vue / AdminKnowledgeIngestView.vue / AdminTicketsView.vue 移除内嵌示例本体；09_API文档.md 移除 device-alarm/geo-vessel 默认分类描述；归档 12_/13_ 旧交接单到 archived/ | Copilot 重构执行 |
| T-028 | 07F 长期可扩展性加固（P0-P3 设计评审落地） | 已完成 | 用户基于"设计缺陷风险 / 性能风险 / 长期可维护性风险"提出 12 条评审；agent 评估 10 条直接吸收、2 条转 KG-FB-007/008 | 07F V0.3 → V0.3.1：§8 Neo4j 由动态 Label 改为固定 `:KnowledgeEntity` + 固定 `:RELATION` + 属性区分（避免 N 租户×M 类型 metadata 爆炸）；§9.4 knowledge_block 新增 `parent_block_id / embedding_model / embedding_version / embedding_dim`；§9.5/§9.6 candidate 状态加 `frozen` + `evidence_refs` 改为带权重对象数组；§10A.4 normalize 改写为 Canonical Resolver 多信号加权（w1 UniqueKey + w2 Alias + w3 Regex + w4 Embedding + w5 CodeGraphRef + w6 LLMVerify，禁止 embedding-only auto-merge），冲突分类扩展为 Cardinality/Attribute/Temporal/Schema/Semantic/Evidence 六类；§11.2 evidence schema 改为 `[{blockId,weight,sourceType}]`；§12.1 状态机加 `published↔frozen` 屏障；§13.2A 新增 Traversal Budget（maxNodes=300/maxEdges=800/maxFanOut=80/maxDepthHardCap=5/timeout=1500ms）；§13.3 Cypher 全面适配 KnowledgeEntity+RELATION 并强制带 budget 参数；§13.4 改为 Hybrid Retrieval（HybridScore = 0.45 Vector + 0.35 BM25 + 0.20 GraphBoost + 可选 0.05 Recency）并新增 tsvector + GIN 索引迁移；§19 加 path/neighbor/evidence 缓存命中率指标；§24 新增 KG-FB-006 Canonical Layer / KG-FB-007 Redis 缓存策略 / KG-FB-008 Retrieval Planner 反馈学习 | Copilot 设计加固 |
| T-029 | 21 任务包对齐 07F V0.3.1 + 数据底座（KG-DB-004/005）落地 | 已完成 | 用户明确"在 21_真实知识图谱构建与检索任务包 目录下完成所有规划任务文档，完成后开始编码，严格参照 07F V0.3.1 不允许偏差" | (1) 21 任务包 9 文件统一加 V0.3.1 章节：02 §2A 增 11 个 P0 任务（KG-DB-004/005、KG-API-004、KG-BE-011~015、KG-AI-006/007、KG-FE-005、KG-QA-005），刷新 §3 优先级与 §4 禁令；01/03/04/05/06/07/08/09/00 各自追加 V0.3.1 章节并列 DoD。(2) DB 底座：real_kg_schema.sql 追加 knowledge_block 4 个 V0.3.1 列 + 生成列 `ts tsvector(simple)` + 3 个新索引；candidate entity/relation status 加 `frozen`；candidate relation 加 `evidence_refs jsonb`；新建 `infra/neo4j/init.cypher`（单 Label 6 索引 4 约束）；挂载到 docker-compose；verify-infra.sh 加 5 项校验并自动 apply init.cypher。完整 docker-compose 联调验证留待下一时段执行 | Copilot 任务规划 + 数据底座 |
| T-030 | 真实 KG V0.3.1 编码启动：API/Java/Python/前端首批落地 | 已完成 | 用户要求"依次全部完成"，承接 KG-API-004、KG-BE-011~015、KG-AI-006/007、KG-FE-005、KG-QA-005 | (1) KG-API-004：09J 增 KG-022/KG-023 freeze/unfreeze，KG-010 增 traversalBudget/budgetUsage/embeddingModel/embeddingVersion，Candidate/Relation evidence 改为加权对象；09C 登记 ICSS-KG-403-FREEZE_FORBIDDEN、409-FROZEN_NODE、413-BUDGET_EXCEEDED、422-EMBEDDING_VERSION_MISMATCH。(2) Java：新增 Neo4jClientConfig、Neo4jGraphRepository（固定 `:KnowledgeEntity` / `[:RELATION]` MERGE 写入 + freeze 状态更新）、FreezeService、GraphObjectActionController；新增 TraversalBudget/Usage + TraversalBudgetExecutor；新增 CanonicalResolver/ScoreBreakdown；新增 KnowledgeBlockRepository + HybridRetrievalService + GraphSearchController/Service；新增 GraphCacheService 5 分钟 TTL path/neighbor/evidence 缓存与 publish/rollback/unfreeze 失效入口。(3) Python：新增 evidence weight、retrieve planner simple_tokenize fallback、canonical_resolver scoreBreakdown；/knowledge/extract、/knowledge/normalize、/graphs/retrieve-plan 返回 V0.3.1 字段。(4) 前端：GraphEditor 增 frozen badge、freeze/unfreeze 操作、evidence 权重标签；新增 BudgetBanner；api.ts 增 freeze/retrieve 调用。(5) QA：新增 CanonicalResolverTest 与 TraversalBudgetTest。验证：Java `mvn -f backend-java/pom.xml test` 成功（5 tests）；Cypher 静态检查 forbidden concrete Label pattern 0 命中；Python `py_compile` 成功；frontend `npm run build` 成功；AI `/graphs/retrieve-plan` 和 `/knowledge/normalize` smoke 成功 | Copilot 编码执行 |
| T-031 | 真实 KG 完整链路打通与数据测试 | 已完成 | 用户要求"将完整链路全部打通，然后进行数据测试" | 完成结构化文本 source 创建后同步执行 parse/extract/graph_build：写入 knowledge_block、candidate_entity、candidate_relation、graph_asset、graph_revision，并写入 Neo4j `:KnowledgeEntity` / `[:RELATION]` draft；前端知识录入页与历史图谱页读取真实 blocks/candidates/graph detail；新增 `scripts/kg-e2e-smoke.sh` 数据冒烟脚本；修复 graph assets SQL `?` 操作符/空参数/`::text` cast 与 JDBC 冲突、Neo4j Map 属性写入、旧 volume graph_category domain CHECK、AI Service 启动依赖检测等问题。验证：`mvn -f backend-java/pom.xml test` 通过（5 tests）；`python3 -m py_compile ai-service-python/app/main.py` 通过；`npm run build:frontend` 通过；`./scripts/kg-e2e-smoke.sh` 通过（blocks=3、entities=2、relations=1、nodes=2、edges=1）；浏览器访问 `/admin/knowledge/ingest` 无 4xx/5xx，预览显示 `KG Smoke Category · 2 个实体 / 1 条关系` | Copilot 全栈执行 |
| T-032 | 真实 KG 分类兜底与来源证据闭环 | 已完成 | 用户指出架构图/流程图等文件可能不属于预设分类，并要求历史图谱和搜索能带出源文件用于可靠性比对 | 新增平台级 `uncategorized / 待归类资料` 兜底分类，知识源未传分类时自动落入待归类；一键启动会重放 PostgreSQL schema/protected taxonomy 到旧 volume，Neo4j schema 重放改为 `APPLY_NEO4J_SCHEMA=1` 可选；图谱详情返回 `sources` 和 `evidenceBlocks`，历史图谱维护页展示关联源文件与证据块；诊断搜索 `vectorEvidence` 返回 `sourceFileName/sourceType/graphCategoryName`；历史图谱列表改用 `nodeCount/edgeCount`。验证：待归类 source `95c83576-1420-4096-98ff-ce0352263f2e` 已 `graph_ready`；图谱详情返回源文件和证据块；浏览器 `/admin/knowledge/graphs` 展示“关联源文件/证据块”；搜索 `ArchitectureFlow` 返回来源文件；`mvn test`、Python compile、frontend build、`scripts/kg-e2e-smoke.sh` 均通过 | Copilot 全栈执行 |
| T-033 | 知识库问答与模型配置隔离最小闭环 | 已完成 | 用户要求问题必须从知识库寻找答案，并要求开启开关后模型必须按照知识库关系图谱回复 | 新增 `ai_model_profile` 表、Profile Repository/Service/Controller；旧知识抽取配置兼容保留但 AI 调用改读 `knowledge_extract` active profile；新增 Java `ChatAnswerService` + `/api/v1/chat/knowledge-answer`，串联 GraphSearch、`chat_answer` active profile 和 AI Service；AI Service 新增 `/chat/answer`，证据不足直接拒答，开启强制图谱时要求 `evidenceRefs`；前端模型配置页改成用途 Tab + 本地/云端独立表单，聊天页新增“强制按知识库关系图谱回答”开关和证据面板。验证：Java 25 package 通过、Python py_compile 通过、frontend build 通过 | Copilot 全栈执行 |

## 3. 最近完成事项

1. 创建 Intelligent Customer Service System 项目目录与文档骨架。
2. 完成产品经理阶段初版文档，包括需求规格、功能拆解、权限矩阵、交付清单与业务流程。
3. 创建 frontend、backend-java、ai-service-python、mcp-server-node、infra 分层工程骨架，并移除早期 apps / packages 试验骨架。
4. 完成 07 系列架构文档，明确分层架构、数据流、部署容量与回流问题。
5. 根据用户最新方向，将主架构从普通 GraphRAG / Code Graph 诊断平台升级为 Unified Engineering Knowledge Graph，补充文档图谱、跨源关联、实体归一、冲突检测和图谱构建 Worker。
6. 根据问答页面草稿补齐产品规划，明确系统执行日志、图谱查看维护、知识库录入、MCP 能力管理、图谱-MCP 映射、诊断路径固化/重建和成功链路更新。
7. 新增数据库/API 设计前 Gate 冻结评审清单，收敛 24 个必答问题、P0 冻结范围、推荐冻结决策和准入标准。
8. 新增数据库/API 设计启动输入，将 Gate 临时结论转换为 P0 表对象、API 分组、字段脱敏、状态流和错误码设计输入。
9. 完成 V0.1 P0 数据库设计，覆盖会话、诊断案例、执行轨迹、知识源、图谱资产、MCP 能力、路径反馈和审计日志。
10. 完成 V0.1 P0 API 设计，覆盖问答会话、诊断进度、执行日志、知识录入、图谱查询、MCP 能力管理、路径反馈和审计查询。
11. 完成 Docker 基础设施验证，PostgreSQL + pgvector、Neo4j、Redis 均可启动和健康检查，PostgreSQL 已验证 `pgcrypto/vector` 扩展与 15 张 P0 表，验证后已关闭容器释放资源。
12. 完成 Java 主后台 Mock API，实现统一响应、错误处理、项目头/幂等头校验、P0 接口和 P1/暂缓接口占位响应，并通过 Maven package 与关键 API smoke。
13. 完成 Vue3 前端联调工作台，实现 API client、问答诊断、诊断结果、执行轨迹、知识录入、MCP 能力和路径反馈页面，并通过 Vite 构建。
14. 完成 Python AI Service Mock 边界，提供诊断推理、知识入图和图谱查询接口，并通过 Java 后端跨服务调用验证。
15. 完成 Node MCP Server 工具调用边界，提供工具 manifest 与 invoke 接口，并通过 Java 后端同步能力和调用 `device.getStatus` 验证。
16. 完成前端工程结构修正，将单体 App.vue 拆分为 router、views、components，并修复一键启动子进程使用旧 Node 导致 Vite 5173 未启动的问题。
17. 根据用户反馈完成前端产品形态重构，将“联调工作台”调整为“用户诊断聊天入口 + 后台管理系统”，后台覆盖知识库/图谱、问题工单和 MCP 工具库启停与影响范围。
18. 根据用户反馈完成知识图谱维护页升级，引入社区图编辑库 Vue Flow，将 JSON 展示替换为关系图画布，并补充结构化文本与非结构化文件入图入口。
19. 修复一键启动脚本过早返回的问题，启动流程现在会等待 5173 前端和后端服务健康接口真正可访问后再提示启动完成。
20. 完成知识图谱分类治理升级，覆盖地区-船舶、船舶-船员、船舶-设备、设备-告警、设备-协议等分类，并从数据库设计、API Mock 到前端页面形成一条线支持。
21. 完成真实知识图谱构建与检索详细设计，明确结构化文本和非结构化文件如何生成真实 Neo4j 图谱、如何结合 pgvector 证据召回、如何发布/回滚/复核，以及后续 Agent 从 0 到 1 的编码任务。
22. 完成真实知识图谱构建与检索专项任务包，拆分 KG-DB、KG-API、KG-BE、KG-AI、KG-FE、KG-QA、KG-CTL 任务，补充模块技术设计和可复制派单提示词。
23. 完成真实 KG 数据库底座开发，新增 8 张 KG 表、8 个图谱分类、28 个实体类型、22 个关系类型和 `knowledge_block` pgvector HNSW 索引，并通过 `./scripts/verify-infra.sh` 验证。
24. 完成真实 KG API 契约补充，新增 09J 文档，覆盖 taxonomy、source、blocks、candidates、review、assets、versions、evidence、diagnosis search，并登记 KG 错误码。
25. 完成真实 KG 后端第一批真实接口落地：`/graphs/taxonomy`、`/graphs/assets`、`/knowledge/sources` 等接口已接入 PostgreSQL，并完成创建知识源与任务落库的 smoke 验证。
26. 完成脏代码扫除：MockSupportService.java 重写为零业务本体的占位实现（删除 graphCategoryItems / graphAssetItems / createKnowledgeSource 等仅 demo 用途的死代码），ai-service-python/app/main.py 重写为五个接口 stub 并指向 KG-AI-001~005，前端 4 个 View 移除内嵌的设备/船舶/告警示例，09_API文档.md 移除 `device-alarm` 等默认分类描述，全仓 grep `TC-003|MINX|AR-17|BOUND_TO|RAISED_BY|INSTALLED_ON|geo-vessel|vessel-device|device-alarm` 在 `backend-java/src/main`、`ai-service-python`、`frontend/src` 三个根下均为 0 命中。
27. 完成 07F §10A 端到端实现章节：补齐非结构化文件 → blocks → candidates 的 4 进程协作时序、4 进程职责切分表、解析阶段 6 条强约束、抽取阶段 6 条强约束、写库阶段 5 条强约束、5 个 Python 接口签名表与失败矩阵，确保任何 Agent 都能据此实现真实管线。
28. 完成 21 任务包归档：将已被 T-023/T-024 完整替代的 `12_数据库Agent交接单.md` 与 `13_接口Agent交接单.md` 移到 `archived/`，并加 README 说明替代关系。
29. 完成真实 KG 完整链路打通与数据测试：结构化文本 source 可生成 blocks、candidate entities/relations、draft graph_asset/revision 和 Neo4j draft，前端知识录入页可直接预览真实关系图，`scripts/kg-e2e-smoke.sh` 已验证 3 blocks、2 entities、1 relation、2 nodes、1 edge。
30. 完成真实 KG 分类兜底与来源证据闭环：未归类文件可进入 `待归类资料`，历史图谱页可查看关联源文件和证据块，诊断搜索结果可带出来源文件名、来源类型和图谱分类，便于把搜索结果与原始文件比对。
31. 完成知识库问答与模型配置隔离最小闭环：`/chat` 调用真实知识库问答接口，模型配置页按 `chat_answer/knowledge_extract` 与 `cloud/local` 完全隔离，强制关系图谱开关开启后无证据不回答、无引用不采纳。

## 4. 当前阻塞事项

| 编号 | 阻塞项 | 原因 | 影响 | 需要谁处理 | 状态 |
| --- | --- | --- | --- | --- | --- |
| B-001 | 真实接口清单缺失 | 目标系统尚未提供完整查询能力清单 | 不阻塞 V0.1 Mock API；阻塞真实 Adapter 替换与生产联调 | 目标系统研发负责人 | 条件推进中 |
| B-002 | 审批流归属未定 | 未明确复用现有审批系统还是自建 | 阻塞 Workflow 与执行闭环设计 | 平台负责人 / 运维负责人 | 阻塞中 |
| B-003 | 权限复用方案未定 | 未确认是否复用 wnx-web SSO/RBAC | 阻塞登录与项目权限设计 | 平台负责人 | 阻塞中 |
| B-004 | 审计与数据保留期未量化 | PM 与业务未给出最终留存期限 | V0.1 暂按 180 天在线留存设计；阻塞最终合规确认 | PM / 业务负责人 | 条件推进中 |
| B-005 | 一期文档图谱来源范围未明确 | 未确认哪些需求、设计、协议、部署、运维、工单附件可脱敏导入 | 阻塞 Source Registry、对象存储、解析任务和入图优先级设计 | PM / 业务负责人 | 阻塞中 |
| B-006 | Cross Link 自动入图和人工复核边界未明确 | 未定义关系置信度阈值、冲突处理和人工确认策略 | 阻塞 Entity Normalize、Conflict Detect、Merge Agent 的 API 与数据库设计 | 架构负责人 / 业务负责人 | 阻塞中 |
| B-007 | 图谱编辑审核和路径固化规则未明确 | 未定义图谱编辑是否双人审核、路径固化粒度、成功/失败判定标准 | 阻塞图谱治理、路径库和反馈模型设计 | PM / 运维负责人 / 研发负责人 | 阻塞中 |
| B-008 | MCP 启停影响范围未明确 | 未定义停用能力时如何确认受影响路径和降级策略 | 阻塞 MCP 管理、路径编排和审计设计 | 平台负责人 / 技术负责人 | 阻塞中 |

## 5. 下一步动作

1. 盘点 wnx-web 与 wnx-serve 的真实查询能力，后续按 capability 逐个替换 Mock Adapter。
2. 在当前真实 KG 最小闭环基础上，继续扩展生产级异步任务 runner、对象存储文件解析/OCR、复核工作流和发布/回滚流。
3. 将 Python AI Service 的通用规则抽取逐步扩展为 parser registry、LLM extractor、embedding 生成和 Hybrid Retrieval planner。
4. 使用 `./scripts/verify-infra.sh` 临时拉起并验证数据库、Neo4j、Redis；验证完成脚本会自动关闭容器。
5. 完成真实 Adapter 前，前端继续只对接 `/api/v1/*`，不得依赖 wnx 原始字段。
6. 真实生产执行动作仍后置，V0.1/V0.2 先保证查询、诊断、图谱治理闭环。
