# Frontend

Vue3 + Element Plus frontend for the smart support console.

This folder is the target code-level boundary for the formal engineering layout:

- diagnosis workspace
- evidence chain view
- approval center
- knowledge graph browser
- audit and operation views

When the UI phase starts, generate the real Vue3 application here and keep it behind the Java backend API gateway.